package year{{ .Year }}

import (
	"fmt"

	"github.com/dhruvmanila/advent-of-code/go/util"
)

func Sol{{ .Day | printf "%02d" }}(input string) (string, error) {
	lines, err := util.ReadLines(input)
	if err != nil {
		return "", err
	}

	return fmt.Sprintf("{{ .Day }}.1: %d\n{{ .Day }}.2: %d\n", 0, 0), nil
}
